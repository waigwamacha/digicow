

## Digicow ML Project

##### Set up

1. Create a .env file in the project root and store a JUPYTER_PASSWORD environment variable. e.g:

```
    JUPYTER_PASSWORD=1234567890
```

2. Run docker and click on the http link outputted in terminal to access the notebooks

```
    docker-compose build 
```

